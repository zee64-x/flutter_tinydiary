class Task {
  String id;
  String title;
  String description;
  DateTime dueDate;

  Task({
    required this.id,
    required this.title,
    required this.description,
    required this.dueDate,
  });
}
